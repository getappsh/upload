export enum RuleType {
  POLICY = 'policy',
  RESTRICTION = 'restriction',
}

export enum RuleFieldType {
  STRING = 'string',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  OBJECT = 'object',
  ARRAY = 'array',
}
