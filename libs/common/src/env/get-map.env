# libot
LIBOT_DISCOVERY_URL=# "The url of the libot discovery service"
LIBOT_EXPORT_URL=# "The url of the libot export service"
LIBOT_CALLBACK_URL=# "The url provided to libot to call back to the service"
TOKEN_LIBOT=# "The token to authenticate with libot"
PROXY_DOWNLOAD_BASE_URL=# "when using proxy server for vpn connection"

MC_CSW_REF_DATE="2023-10-15T00:00:01Z" # "The reference date for the filter products"
TARGET_RESOLUTION=17 # "The target resolution for filtering products"
MC_MIN_RESOLUTION_DEG="4.29153E-05" # "The minimum resolution for exporting maps"
MC_MAX_RESOLUTION_DEG="1.34110E-06" # "The maximum resolution for exporting maps"

# # Polygon pars wfs
PRODUCTS_CREATION_METHOD=# "single / polygonParts - set the method to create products, default is single"
POLYGON_PARTS_WFS=# "The url of the polygon parts wfs service"
MANEUVER_AREA=# "The maneuver area for the polygon parts method, in the format of 'lat,lon,lat,lon,...'"
SEQUENTIAL_PRODUCT_ID=# "The sequential product id for the polygon parts method, in format of 'productId-productType'"

# ##### server configs
UPDATE_GOB_TIME='0 0 */6 * * *' # "The cron time to update the GOB, default is every 6 hours"
PRODUCTS_STALE_AFTER=# "The time in minutes to consider a product stale, default is 3 minute"
USE_PRODUCTS_CACHE=# "A flag if using cache for products, default is true"
PERIODIC_GET_MAP_STATUS=# "Seconds to set the periodic time to request status for map  --- default is 3 seconds"
MAP_RETRY_EXPONENTIAL_TIMES=# "The exponential times for retrying the request in array, default is [0.3, 5, 15]"
MAP_RETRY_WAIT_TIME=# "The time to wait between retries, default is 0.3"
MAP_RETRY_COUNT=# "The number of retries, default is 3"
UPDATE_JOB_MAP_TAKE=# "The number of maps to take in each update job, default is 25"


# ## emulator
LIBOT_EMULATOR=# "true / false - set if use an emulator to emulate libot"